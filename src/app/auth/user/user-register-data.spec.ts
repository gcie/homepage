import { UserRegisterData } from './user-register-data';

describe('UserRegisterData', () => {
    it('should create an instance', () => {
        expect(new UserRegisterData()).toBeTruthy();
    });
});
